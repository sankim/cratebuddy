{
    "build": { "builder": "NIXPACKS" },
    "deploy": {
      "startCommand": "gunicorn app:app --chdir server --bind 0.0.0.0:$PORT --workers 2 --threads 4",
      "healthcheckPath": "/healthz",
      "restartPolicyType": "ON_FAILURE"
    }
  }
  